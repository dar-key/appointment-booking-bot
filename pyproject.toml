[project]
name = "service-booking-bot"
version = "0.1.0"
description = "Telegram booking bot integrated with Google Sheets"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.12.0",
    "google-auth>=2.30.0",
    "gspread-asyncio>=2.0.0",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.0",
]
